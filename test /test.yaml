esphome:
  name: xy6020_Limiter
  #friendly_name: xy6020_Limiter
  comment: "XY6020 0-Watt Limiter"

esp8266:
  board: esp8285

ota:
  password: !secret xyota

api:
  encryption:
    key: !secret xykey

captive_portal:

logger:
  level: DEBUG

packages:
  remote_package:
    url: https://github.com/creepystefan/esphome-XY6020
    refresh: 0d
    files: [
      test/core.yaml,
      test/ICHI_smartmeter.yaml,
      test/sensors.yaml,
      test/switches.yaml,
      Language/german.yaml,
        ]
