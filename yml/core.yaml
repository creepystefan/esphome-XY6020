logger:
  level: DEBUG

# Enable Home Assistant API
api:
  encryption:
    key: !secret xykey
ota:
  password: !secret xyota

packages:
  # Minimal config
  adress: !include adress.yaml
  switches: !include switches.yaml
  sensors: !include sensors.yaml
  textsensors: !include textsensors.yaml
  numbers: !include numbers.yaml
  #core: !include core.yaml

captive_portal:

#<<: !include modbus.yaml
#<<: !include sensors.yaml
#<<: !include numbers.yaml
#<<: !include switches.yaml
#<<: !include textsensors.yaml

